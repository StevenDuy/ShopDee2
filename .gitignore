# Cấu hình Bỏ qua (Ignore) cho ShopDee2

# Frontend (Next.js)
/frontend/node_modules
/frontend/.next/
/frontend/out/
/frontend/build/

# Backend (Laravel)
/backend/vendor/
/backend/public/build/
/backend/public/hot
/backend/public/storage
/backend/storage/*.key

# Quy tắc chặn file môi trường thật (Sẽ không đẩy lên Git)
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local

# QUY TẮC PHỦ QUYẾT (Whitelist): Luôn hiển thị file mẫu .example
!.env.example
!.env.local.example

# File hệ thống
.DS_Store
Thumbs.db
*.log

# Bỏ qua phần mềm trợ lực (Cloudflared)
cloudflared.exe

# Bỏ qua cấu hình tunnel cá nhân (chứa credentials path)
# Mỗi máy sẽ tự tạo file này theo hướng dẫn README
# cloudflare-config.yml  ← (bỏ comment nếu muốn share config ra team)
gemini_history.md
shopdee_architecture_2.md
shopdee_old_analysis.md
knowledge_base.md
KNOWLEDGE_TRANSFER.md
DEVELOPER_GUIDELINES.md
